\subsection{Prevalence}\label{subsec:prevalence}

\sg{add plot on the 11 users that changed behavior - but need to explain the stats without these aggressive users}

\sg{it'll be awesome to add persistence to this plot after segregating rich greedy and rich frugals and reporting stats - easily done in pandas}
